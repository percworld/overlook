import Customer from './Customer';
import $ from 'jquery';

let hotel;

const updates = {

  onLoad: (repoHotel) => {
    hotel = repoHotel;
    //$('.sdgd').html(    )


  }
}

export default updates;
